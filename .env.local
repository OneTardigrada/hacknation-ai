# OpenAI API Key — required for live offer generation
# Without this key, fallback offers are used (demo still works fully)
OPENAI_API_KEY=

# OpenWeatherMap API Key — optional, mock data used if absent
OPENWEATHER_API_KEY=

# City config — "stuttgart" (default) or "munich"
NEXT_PUBLIC_CITY_CONFIG=stuttgart
